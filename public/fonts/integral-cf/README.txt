Place your Integral CF font files in this folder with these filenames:

- IntegralCF-Regular.woff2 (or .woff)
- IntegralCF-ExtraBold.woff2 (or .woff)

The app is already configured to load these files via @font-face in src/app/globals.css.
